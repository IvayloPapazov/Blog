{% block user_block %}

<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <span class="glyphicon glyphicon-user" ></span> 
                {{ app.user.username }} 
            <span class="caret"></span>
        </a>
      <ul class="dropdown-menu" role="menu">
        <li><a href="{{ path('create_post') }}">
            <span class="glyphicon glyphicon-pencil"></span> {{'menu.new'|trans}}</a>
        </li>
        <li><a href="{{ path('user_post') }}">
            <span class="glyphicon glyphicon-th-list"></span>
             {{ 'menu.myPost'|trans }} 
            <span class="badge"> 
                {{ app.user.postCount }}
            </span></a>
        </li>
        <li><a href="{{ path('edit') }}">
            <span class="glyphicon glyphicon-user"></span> {{'menu.myProfile'|trans}}</a>
        </li>
        <li><a href="{{ path('show_posts') }}">
            <span class="glyphicon glyphicon-wrench"></span> {{'menu.userDashboard'|trans}}</a>
        </li>
        <li class="divider"></li>
        <li><a href="{{ path('logout') }}">
            <span class="glyphicon glyphicon-off"></span> {{'menu.logout'|trans}} </a>
        </li>
    </li>
</ul>

{% endblock %}